@model EPAM_Internet_Provider.Domain.Models.Subscription

@{
    ViewBag.Title = "UnsubscribeFromRate";
}

<h2>Unsubscribe From Rate</h2>

<h3>Are you sure you want to unsubscribe this?</h3>
<div>
    <hr />
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Unsubscribe" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
